import React, { Component } from 'react';
import InputText from './../../Components/InputText';
import PropTypes from 'prop-types';
import globalValues from './../../globalState';


export default class FullNameNB extends Component {
    static get propTypes () {
        return{
            getRef:PropTypes.func,
            onSubmitEditing:PropTypes.func,
            returnKeyType:PropTypes.string.isRequired
        }
    }
    constructor(props){
        super(props);
        this.state={
            FullName:''
        }
    }
    changeFirstNameText(e){
        if(this.state.FullName == ''){
            var string1 = e.replace(/\s/g, '');
        }
        else{
            var string1 = e
        }
        this.setState({FullName:string1},()=>{
            var FullName = this.state.FullName
            globalValues.FullName = FullName
        });
    }
    render() {
        return(
            <InputText onChangeText={this.changeFirstNameText.bind(this)} secureTextEntry={false}
                       getRef={this.props.getRef} onSubmitEditing={this.props.onSubmitEditing}
                       style={this.props.style}
                       returnKeyType = {this.props.returnKeyType} value={this.state.FullName}>Full Name</InputText>
        );
    }
}
